export const META_GRAPH_VERSION = "v21.0";
const LEAD_ACTION_PRIORITY = ["lead", "onsite_conversion.lead_grouped", "offsite_conversion.fb_pixel_lead", "onsite_conversion.messaging_conversation_started_7d"];
type Action = { action_type?: string; value?: string };
export type DailyAdInsight = { date_start?: string; campaign_id?: string; campaign_name?: string; adset_id?: string; adset_name?: string; ad_id?: string; ad_name?: string; spend?: string; impressions?: string; reach?: string; clicks?: string; ctr?: string; cpc?: string; cpm?: string; actions?: Action[] };
const number = (value: unknown) => { const parsed = Number(value ?? 0); return Number.isFinite(parsed) ? parsed : 0; };
export function resolveMetaLeads(actions: Action[] = []) { for (const actionType of LEAD_ACTION_PRIORITY) { const action = actions.find((item) => item.action_type === actionType && number(item.value) > 0); if (action) return { leads: number(action.value), lead_action_type: actionType }; } return { leads: 0, lead_action_type: null }; }
export function normalizeDailyAdInsight(insight: DailyAdInsight, context: { cliente_id: string; meta_account_id: string; currency: string; account_timezone: string; synced_at: string }) { if (!insight.date_start || !insight.campaign_id || !insight.adset_id || !insight.ad_id) return null; const lead = resolveMetaLeads(insight.actions); return { ...context, metric_date: insight.date_start, campaign_id: insight.campaign_id, campaign_name: insight.campaign_name ?? null, adset_id: insight.adset_id, adset_name: insight.adset_name ?? null, ad_id: insight.ad_id, ad_name: insight.ad_name ?? null, spend: number(insight.spend), impressions: number(insight.impressions), reach: number(insight.reach), clicks: number(insight.clicks), ctr: number(insight.ctr), cpc: number(insight.cpc), cpm: number(insight.cpm), ...lead, source: "meta_marketing_api", api_version: META_GRAPH_VERSION }; }
